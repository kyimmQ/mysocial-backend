/**
 * AUTH ROUTES
 *
 * Defines HTTP endpoints for the auth feature.
 *
 * Pattern:
 *   POST /signup     → SignUpController
 *   POST /signin     → SignInController
 *   POST /signout    → SignOutController
 *   POST /forgot-password  → ForgotPasswordController
 *   POST /reset-password   → ResetPasswordController
 *   GET  /currentuser      → CurrentUserController
 *
 * Exported as a Router, mounted by the central route registry.
 */
