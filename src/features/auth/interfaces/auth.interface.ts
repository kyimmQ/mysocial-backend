/**
 * AUTH INTERFACES
 *
 * TypeScript types for the auth feature.
 * These define the "shape" of data — no implementation.
 *
 * Examples:
 *   - IAuthDocument (email, username, password, avatarColor, createdAt)
 *   - ISignUpData (request body shape)
 *   - ISignInData (request body shape)
 *   - IAuthJob (queue job payload)
 */
